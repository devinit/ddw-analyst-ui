import React, { ReactElement } from 'react';
import { Row } from 'react-bootstrap';

const DataSourceQuery = (): ReactElement => {
  return (
    <Row>
      <h1>DataSourceQuery</h1>
    </Row>
  );
};

export default DataSourceQuery;
